import { MediaPlaceholder } from "@/components/ui/media-placeholder";

export function IllustrationBand() {
  return (
    <section className="px-0 py-[2rem]">
      <MediaPlaceholder
        ratio="16 / 5"
        className="w-full rounded-none"
        label="Ilustración panorámica — patrón Rautenmuster pendiente"
      />
    </section>
  );
}
